const initialState = {
    moviesList: [],
    currentPage: 1,
    filters: {}
}

export const movies = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}